/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package tspeaklibtest;

/**
 *
 * @author markf
 */

import TSpeakLib.*;

public class Main {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        
        connectionTest();
        
    }
    
    static void connectionTest()
    {
        ClientConnection a_connection = new ClientConnection("ts.deadcodeelimination.com",8767);
        
        a_connection.sendConnectionPacket1("","lionftw","Albatross");
        
        int count = 0;
        
        while(true) //loop forever and ever :)
        {
            if (count == 4)
            {
                a_connection.sendTextMessage("I'm a pink tooth brush", 2);
            }
            
            a_connection.recieveServerPacket();
            
            count++;
        }
           
    }

}
